#include "main.h"

/**
 * more_numbers - prints 0 - 14 ten times
 *
 * Return: void
 */
void more_numbers(void)
{
	int c;

	int count;

	for (c = 0; c <= 14; c++)
		for (count = 0; count <= 10; count++)
        {
            if (c >= 10)
                _putchar('1');
                _putchar(i % 2 + '0');
                _putchar('\n');
        }
